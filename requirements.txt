# Web UI
# 1.56 keeps native st.login() auth cookies persistent for 30 days.
streamlit[auth]==1.56.0

# Cookie 持久化 session（30 天免重新登入）
extra-streamlit-components>=0.1.71

# Google Auth & APIs
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0
gspread>=6.1.0

# Playwright（Headless 瀏覽器）
playwright>=1.44.0

# AI
google-genai>=0.8.0

# Data
# Keep native wheels on the Python 3.12 deployment baseline. Unbounded
# upgrades previously resolved Python 3.14 + pandas 3/pyarrow 25 and the
# Streamlit launcher terminated with SIGSEGV before app.py was imported.
numpy==2.2.6
pandas==2.2.3
pyarrow==18.1.0
openpyxl>=3.1.0
reportlab==4.4.3
qrcode[pil]>=7.4.2
pillow>=10.0.0
pypdf>=4.2.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
